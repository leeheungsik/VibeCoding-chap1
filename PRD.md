# PRD: 할 일 관리 앱 (Personal To-Do App)

## 1. 개요

### 1.1 배경
하루 10~20개 정도의 할 일을 관리하는 개인용 To-Do 앱. 복잡한 협업 기능 없이, 혼자서 빠르게 할 일을 등록하고 처리 상황을 확인하는 데 집중한다.

### 1.2 목표
- 할 일을 30초 이내에 추가할 수 있을 만큼 입력 과정이 가볍다.
- 오늘 해야 할 일과 완료한 일을 한눈에 구분할 수 있다.
- 업무/개인/공부로 할 일을 나눠 보고, 하루 진행률을 확인할 수 있다.

### 1.3 타겟 사용자
- 하루 10~20개 수준의 할 일을 개인적으로 관리하는 1인 사용자
- 팀 공유, 알림, 협업 기능이 필요하지 않은 사용자

## 2. 범위

### 2.1 In Scope (이번 버전에 포함)
- 할 일 추가 / 수정 / 삭제
- 완료 체크 (완료/미완료 토글)
- 카테고리 분류 (업무 / 개인 / 공부)
- 진행률 보기 (완료 개수 / 전체 개수, 카테고리별 진행률 포함)
- 데이터 내보내기 / 가져오기 (JSON 형식)

### 2.2 Out of Scope (이번 버전 제외)
- 다중 사용자 계정, 로그인/회원가입
- 실시간 협업, 공유 기능
- 푸시 알림, 리마인더
- 반복 일정 (매일/매주 반복 할 일)
- 서브태스크(하위 할 일)
- 첨부파일, 댓글
- 모바일 네이티브 앱 (우선 웹 기준으로 정의, 반응형 고려는 선택)

> 위 항목들은 v1 이후 사용자 피드백에 따라 재검토한다.

### 2.3 기술 제약사항
- 별도 설치나 빌드 과정 없이 브라우저에서 바로 실행된다 (HTML/CSS/순수 JavaScript, 프레임워크·빌드 도구 미사용).
- 서버나 백엔드 없이 동작하는 클라이언트 전용 앱이다.
- 브라우저 `localStorage`를 사용해 데이터를 저장하며, 새로고침하거나 브라우저를 다시 열어도 입력한 할 일이 유지된다.

## 3. 주요 기능 정의

### 3.1 할 일 추가
- 사용자는 제목(필수)을 입력해 할 일을 추가할 수 있다.
- 추가 시 카테고리(업무/개인/공부)를 선택할 수 있다. 선택하지 않으면 기본값은 "개인"으로 한다.
- 제목은 최소 1자 이상이어야 하며, 공백만 입력한 경우 저장되지 않는다.
- 새로 추가된 할 일은 기본적으로 "미완료" 상태로 생성된다.

### 3.2 할 일 수정
- 사용자는 기존 할 일의 제목과 카테고리를 수정할 수 있다.
- 수정 중 취소가 가능해야 하며, 취소 시 원래 값이 유지된다.

### 3.3 할 일 삭제
- 사용자는 할 일을 개별적으로 삭제할 수 있다.
- 삭제는 되돌릴 수 없는 동작이므로, 삭제 전 확인 절차(예: 확인 다이얼로그)를 거친다.

### 3.4 완료 체크
- 사용자는 체크박스(또는 이에 준하는 UI)로 할 일의 완료 여부를 토글할 수 있다.
- 완료된 할 일은 시각적으로 구분되어 표시된다 (예: 취소선, 흐린 색상).
- 완료 상태는 목록 정렬이나 필터링(전체/진행중/완료)에 영향을 줄 수 있다.

### 3.5 카테고리 분류
- 카테고리는 업무 / 개인 / 공부 3종으로 고정한다 (v1 기준, 커스텀 카테고리는 제외).
- 목록 화면에서 카테고리별로 필터링해서 볼 수 있다.
- 각 할 일은 정확히 하나의 카테고리에 속한다.

### 3.6 진행률 보기
- 전체 할 일 대비 완료한 할 일의 비율을 진행률로 표시한다 (예: "8/15 완료, 53%").
- 카테고리별 진행률도 함께 확인할 수 있다 (예: 업무 3/5, 개인 4/7, 공부 1/3).
- 진행률은 할 일이 추가/삭제/완료 처리될 때마다 실시간으로 갱신된다.

### 3.7 데이터 내보내기 / 가져오기
- 사용자는 현재 할 일 목록 전체를 JSON 파일로 내보낼 수 있다 (예: `todos-2026-08-20.json`).
- 사용자는 내보낸 JSON 파일을 다시 가져와 할 일 목록에 추가할 수 있다.
- 가져오기는 기존 할 일 목록을 덮어쓰지 않고 추가(병합)한다. 데이터 유실 없이 여러 기기/브라우저 간 백업·이전 용도로 사용한다.
- 형식이 올바르지 않은 파일을 가져오려 하면 사용자에게 안내 메시지를 표시하고 기존 데이터는 변경하지 않는다.

## 4. 사용자 스토리

| ID | 사용자 스토리 | 우선순위 |
|----|----------------|----------|
| US-1 | 사용자로서, 새로운 할 일을 빠르게 추가하고 싶다 | High |
| US-2 | 사용자로서, 할 일에 카테고리를 지정해 분류하고 싶다 | High |
| US-3 | 사용자로서, 완료한 할 일을 체크로 표시하고 싶다 | High |
| US-4 | 사용자로서, 할 일의 제목이나 카테고리를 수정하고 싶다 | Medium |
| US-5 | 사용자로서, 더 이상 필요 없는 할 일을 삭제하고 싶다 | Medium |
| US-6 | 사용자로서, 오늘 얼마나 처리했는지 진행률로 확인하고 싶다 | High |
| US-7 | 사용자로서, 카테고리별로 할 일을 필터링해서 보고 싶다 | Medium |
| US-8 | 사용자로서, 할 일 목록을 JSON 파일로 내보내고 다시 가져와 백업/복원하고 싶다 | Low |

## 5. 화면 구성 (초안)

1. **메인 목록 화면**
   - 상단: 전체 진행률 표시 (예: 프로그레스 바 + "8/15 완료")
   - 카테고리 필터 탭 (전체 / 업무 / 개인 / 공부)
   - 할 일 목록 (체크박스, 제목, 카테고리 태그, 수정/삭제 버튼)
   - 할 일 추가 입력창 (제목 입력 + 카테고리 선택 + 추가 버튼)
2. **수정 모드**
   - 목록 내 인라인 편집 또는 별도 모달로 제목/카테고리 수정

## 6. 데이터 모델 (초안)

```
Todo {
  id: string
  title: string
  category: "업무" | "개인" | "공부"
  isCompleted: boolean
  createdAt: datetime
  updatedAt: datetime
}
```

할 일 목록은 배열 형태로 직렬화(JSON)하여 `localStorage`의 단일 키(예: `todos`)에 저장한다. 추가/수정/삭제/완료 토글 등 데이터 변경 시마다 즉시 저장한다.

## 7. 성공 지표 (Success Metrics)

- 할 일 추가에 걸리는 조작 횟수(클릭/입력) 최소화
- 하루 평균 할 일 완료율 확인 가능 여부
- 사용자가 앱을 매일 열어 할 일을 관리하는지 (개인 사용 빈도)

## 8. 향후 고려 사항 (Future Considerations)

- 마감일/시간 지정
- 반복 할 일
- 우선순위 태그 (긴급/중요 등)
- 데이터 로컬 저장 vs 클라우드 동기화
- 다크 모드
