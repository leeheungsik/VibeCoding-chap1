# Claude Code 단계별 실행 프롬프트

`PRD.md`를 기반으로 할 일 관리 앱을 만들 때 Claude Code에 순서대로 입력할 5단계 프롬프트입니다. 각 단계는 이전 단계의 결과물 위에서 동작하도록 구성되어 있으며, 한 단계씩 실행하고 결과를 확인한 뒤 다음 단계로 넘어가는 것을 권장합니다.

---

## 1단계 — 프로젝트 뼈대 구성

```
PRD.md를 읽고 이 프로젝트의 기술 제약사항(순수 JavaScript, 빌드 도구 없음, 브라우저에서 바로 실행, localStorage로 데이터 유지)을 확인해줘.

이 요구사항에 맞춰 다음 파일들의 기본 뼈대를 만들어줘:
- index.html: 제목 영역, 진행률 표시 영역, 카테고리 필터 탭, 할 일 목록 영역, 할 일 추가 입력 폼을 위한 마크업 (기능은 아직 없어도 됨)
- style.css: 전체 레이아웃과 기본 타이포그래피, 여백 등 기초 스타일
- app.js: localStorage에서 할 일 배열을 읽고 쓰는 로드/저장 함수(loadTodos, saveTodos)만 먼저 구현. 아직 화면 렌더링 로직은 만들지 마.

완료되면 index.html을 브라우저에서 열어 레이아웃이 깨지지 않고 보이는지 스크린샷으로 확인해줘.
```

---

## 2단계 — 할 일 추가·수정·삭제 (CRUD)

```
PRD.md의 "3.1 할 일 추가", "3.2 할 일 수정", "3.3 할 일 삭제" 요구사항대로 app.js에 다음 기능을 구현해줘:

- addTodo(title, category): 제목이 비어있거나 공백만 있으면 추가하지 않음. 기본 카테고리는 "개인". 추가된 할 일은 localStorage에 즉시 저장.
- updateTodo(id, changes): 제목과 카테고리를 수정. 수정 중 취소 시 원래 값 유지.
- deleteTodo(id): 삭제 전 확인 절차(confirm 다이얼로그)를 거친 뒤 삭제하고 localStorage에 반영.
- 위 함수들과 연결된 화면 렌더링(목록 표시, 추가 폼 제출 처리, 수정 UI, 삭제 버튼)도 함께 구현해줘.

구현 후 브라우저에서 할 일을 3개 추가하고, 1개를 수정하고, 1개를 삭제하는 과정을 직접 실행해서 각 단계 스크린샷을 남기고, 새로고침해도 남은 할 일이 유지되는지 확인해줘.
```

---

## 3단계 — 완료 체크 기능

```
PRD.md의 "3.4 완료 체크" 요구사항대로 구현해줘:

- 각 할 일 항목에 체크박스(또는 동등한 UI)를 추가하고, 클릭 시 완료/미완료 상태를 토글하는 toggleComplete(id) 함수를 app.js에 구현.
- 완료된 할 일은 CSS로 시각적으로 구분되게 해줘 (예: 취소선, 흐린 색상).
- 완료 상태 변경 즉시 localStorage에 저장되어야 해.

구현 후 브라우저에서 할 일 하나를 완료 처리하고, 취소선이 적용되는지, 새로고침 후에도 완료 상태가 유지되는지 스크린샷으로 확인해줘.
```

---

## 4단계 — 카테고리 분류 및 필터

```
PRD.md의 "3.5 카테고리 분류" 요구사항대로 구현해줘:

- 할 일 추가/수정 시 업무, 개인, 공부 중 하나의 카테고리를 선택할 수 있게 해줘 (이미 2단계에서 일부 구현했다면 UI를 다듬는 수준으로).
- 목록 상단에 "전체 / 업무 / 개인 / 공부" 필터 탭을 만들고, 클릭 시 해당 카테고리의 할 일만 보이도록 필터링 로직을 구현해줘.
- 각 할 일 항목에 카테고리를 시각적으로 구분해서 표시해줘 (예: 색상 태그).

구현 후 브라우저에서 서로 다른 카테고리의 할 일을 추가하고, 각 필터 탭을 클릭했을 때 올바른 항목만 보이는지 스크린샷으로 확인해줘.
```

---

## 5단계 — 진행률 표시 및 최종 점검

```
PRD.md의 "3.6 진행률 보기" 요구사항대로 구현해줘:

- 전체 할 일 대비 완료한 할 일의 비율을 "완료 개수/전체 개수 (퍼센트)" 형태로 화면 상단에 표시.
- 프로그레스 바(시각적 바) 형태로도 진행률을 보여줘.
- 업무/개인/공부 카테고리별 진행률도 함께 표시해줘.
- 할 일이 추가/삭제/완료 처리될 때마다 진행률이 실시간으로 갱신되어야 해.

구현이 끝나면 PRD.md의 4가지 핵심 기능(추가·수정·삭제, 완료 체크, 카테고리 분류, 진행률 보기)을 모두 브라우저에서 실제로 조작해보면서 전체 흐름을 점검해줘:
1. 할 일 5개 이상 추가 (카테고리 섞어서)
2. 일부 완료 처리
3. 카테고리 필터 전환
4. 할 일 수정/삭제
5. 새로고침 후 모든 상태가 그대로 유지되는지 확인
6. 콘솔에 에러가 없는지 확인

문제가 있으면 수정하고, 최종 결과 스크린샷을 남겨줘.
```

---

## 사용 팁
- 각 단계 프롬프트는 그대로 복사해서 Claude Code에 입력하면 됩니다.
- 한 단계가 끝나면 실제로 동작을 확인한 뒤 다음 단계로 넘어가세요. 문제가 발견되면 다음 단계로 넘어가기 전에 "방금 만든 기능에서 OO 문제가 있어, 고쳐줘"처럼 바로 피드백을 주면 됩니다.
- 이 저장소에는 이미 위 5단계가 모두 반영된 완성본(`index.html`, `style.css`, `app.js`)이 있습니다. 처음부터 다시 만들어보거나, 다른 프로젝트에 동일한 흐름을 적용할 때 참고용으로 사용하세요.
